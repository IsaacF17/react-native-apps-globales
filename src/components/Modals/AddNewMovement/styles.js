import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  addNewMovementModal: {
    alignSelf: 'center',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
  },
});
